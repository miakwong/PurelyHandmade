# Direct database connection settings (local development)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=no15438
# DB_PASSWORD=158795
# DB_NAME=purely_handmade
# DB_SOCKET=/opt/homebrew/var/mysql/mysql.sock

# Database connection settings (SSH Server)
DB_HOST=localhost         # The database server hostname (localhost means the same server)
DB_PORT=3306             # The port number for MySQL/MariaDB (default is 3306)
DB_USER=miakuang         # !!The database username = cwl
DB_PASSWORD=miakuang     # !!The database password = cwl
DB_NAME=miakuang         # !!The name of the database to be used = cwl
DB_SOCKET=/run/mysqld/mysqld.sock  # Path to the MySQL/MariaDB socket file (used for local connections)

# JWT (JSON Web Token) settings
JWT_SECRET=purely-handmade-secret-key-for-jwt-tokens  # Secret key used to sign JWT tokens for authentication
JWT_EXPIRY=24h    # Expiration time for JWT tokens (24 hours)

# File permission command (for reference)
# !! chmod 644 /home/cwl/public_html/PurelyHandmade/.env 
# This command sets read/write permissions for the owner and read-only for others (644)

# !! Test: php -r 'var_dump(parse_ini_file("/home/cwl/public_html/PurelyHandmade/.env"));'
# Test return array

# Change .env with no comments
